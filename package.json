{
  "name": "taskpilot-ai",
  "version": "1.0.0",
  "description": "GenAI Developer Productivity Platform",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && ESLINT_NO_DEV_ERRORS=true ESLINT_CACHE=false DISABLE_ESLINT_PLUGIN=true npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["genai", "developer", "productivity"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

